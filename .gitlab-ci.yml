stages:
  - test
  - deploy

test:
  stage: test
  script:
    - echo "Who am I?"
    - whoami

deploy:
  stage: deploy
  script:
    - cp -R . /home/dbsa215/server/
    - rsync -av --progress . /home/dbsa215 --exclude .git
  only:
    - master
